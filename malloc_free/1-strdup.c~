#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *
 *
 *
 *
 */
char *_strdup(char *str)
{
  char *newstr;
  newstring = malloc(sizeof(char) * str);
    if (str = NULL)
      return (NULL);
	
