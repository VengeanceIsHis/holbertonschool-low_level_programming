#include <stdio.h>
#include "dog.h"
/**
 *
 *
 *
 */
void print_dog(struct dog *d)
{
  if (d = NULL)
    {
printf("(nil)"
}
printf("Name: %s", d->name);
  printf("Age: %s", d->age);
  printf("Owner: %s", d->owner);
}
