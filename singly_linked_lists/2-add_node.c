#include "lists.h"
#include <stdio.h>
/**
 *
 *
 */
list_t *add_node(list_t **head, const char *str)
{
		list_t push;
		struct list_s->str = str;
		struct list_s->head = (*head);
		(*head) = list_s;
}
