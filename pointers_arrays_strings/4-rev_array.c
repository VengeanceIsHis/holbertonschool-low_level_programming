#include "main.h"
/**
 *
 *
 *
 *
 */
void reverse_array(int *a, int n)
{
  int i;
  while (int *a[i] = '\0' && i <= n)
    {
      i++;
    }
  while (int *a[i] <= 0)
    {
      putchar(*a[i]);
	i--;
    }
}
