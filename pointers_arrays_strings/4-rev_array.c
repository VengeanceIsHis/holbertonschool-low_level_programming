#include "main.h"
/**
 *
 *
 *
 *
 */
void reverse_array(int *a, int n)
{
  int i;
  while (*a[i] = '\0' && i <= n)
    {
      i++;
    }
  while (*a[i] <= 0)
    {
      putchar(*a[i])
	i--;
    }
}
