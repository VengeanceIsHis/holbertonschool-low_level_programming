#include "main"
/**
 *add -adds two integers and returns result
 *@i: integer to be added to i
 *@j: integer to be added to j
 *Return: sum of a and b
 */
int add(int i, int j)
{
int k;
k = i + j;
return (k);
}
