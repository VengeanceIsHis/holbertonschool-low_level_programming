#include <stdio.h>

int main(void)
{
  for (x = 1; x <= 100; x++)
      if (x % 3 == 0)
	{
	printf("Fizz");

	}
      else if
	{
	  (x % 3 == 0 || x % 5 == 0)
	  printf("Fizz Buzz");
	}
      else if
	{
	    (x % 5 == 0)
	    printf("Buzz");
	}
	    else
	      {
		printf("%d", x);
	      }
}
