#include "main.h"
/**
 *mul - multiplies two integers together
 *@a: first integer to be multiplied
 *@b: second integer to be multiplied
 *Return: the answer between the two
 */
int mul(int a, int b)
{
return (a * b);
}
